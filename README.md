# swx-tensorflow-android

This is a docker build harness to compile the tensorflow android client from source.

## Usage:

    docker-compose up

Then look in the `outputs/` folder for the apk files.

